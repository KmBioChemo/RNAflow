# Tidy enrichment label

Strips collection prefixes (e.g. "HALLMARK\_") and turns underscores
into spaces for readable axis labels.

## Usage

``` r
clean_term(x)
```

## Arguments

- x:

  character vector of term names
