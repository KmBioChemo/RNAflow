# Continuous fill scale for scores (sequential)

Continuous fill scale for scores (sequential)

## Usage

``` r
scale_fill_omics_c(palette = "batlow", ...)

scale_color_omics_c(palette = "batlow", ...)
```

## Arguments

- palette:

  scico palette name

- ...:

  passed to
  [`ggplot2::scale_fill_gradientn()`](https://ggplot2.tidyverse.org/reference/scale_gradient.html)
