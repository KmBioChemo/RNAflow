# Create an empty project state

Create an empty project state

## Usage

``` r
empty_project(name = "untitled")
```

## Arguments

- name:

  project name (used as default filename)

## Value

a list with the canonical project structure
