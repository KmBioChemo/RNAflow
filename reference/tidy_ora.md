# Normalize a clusterProfiler enrichResult to a tidy data.frame

Normalize a clusterProfiler enrichResult to a tidy data.frame

## Usage

``` r
tidy_ora(er)
```

## Arguments

- er:

  an enrichResult object (or NULL)

## Value

a data.frame with standardized columns (possibly zero rows)
