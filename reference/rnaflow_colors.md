# Qualitative RNAflow colors

Qualitative RNAflow colors

## Usage

``` r
rnaflow_colors(n)
```

## Arguments

- n:

  number of colors needed

## Value

a character vector of `n` hex colors (interpolated if `n` exceeds the
base palette)
