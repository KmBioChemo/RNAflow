# Color picker with synchronized hex text input

Color picker with synchronized hex text input

## Usage

``` r
ui_color_picker(id, lbl, default)
```

## Arguments

- id:

  input ID (Shiny will see `input[[id]]` with the hex value)

- lbl:

  display label

- default:

  starting hex color

## Value

a UI element
