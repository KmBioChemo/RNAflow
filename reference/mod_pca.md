# PCA module

PCA module

## Usage

``` r
mod_pca_ui(id)

mod_pca_server(id, counts_reactive, metadata_reactive)
```

## Arguments

- id:

  namespace ID

- counts_reactive:

  reactive for counts matrix (normalized recommended)

- metadata_reactive:

  reactive for metadata
