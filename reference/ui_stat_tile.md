# Coloured stat tile (big number + label)

Coloured stat tile (big number + label)

## Usage

``` r
ui_stat_tile(n, lbl, bg = "#1D9E75")
```

## Arguments

- n:

  the number / value to show

- lbl:

  the caption under it

- bg:

  background colour (hex)

## Value

a `div` tag
