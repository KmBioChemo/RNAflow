% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_wgcna.R
\name{with_wgcna_cor}
\alias{with_wgcna_cor}
\title{Run an expression while WGCNA's \code{cor} shadows \code{stats::cor}}
\usage{
with_wgcna_cor(expr)
}
\arguments{
\item{expr}{an expression to evaluate}
}
\description{
Several WGCNA routines call \code{do.call("cor", ...)} with WGCNA-specific
arguments (\code{weights.x}, \code{cosine}, ...). When the WGCNA package is not on
the search path (the normal case inside this package / a Shiny app), that
bare lookup resolves to \code{stats::cor} and errors. Binding \code{cor} to
\code{WGCNA::cor} on the global environment for the duration of the call is the
documented workaround; we restore the previous binding on exit.
}
\keyword{internal}
