% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_de.R
\name{run_deseq2}
\alias{run_deseq2}
\title{Run DESeq2 on a counts matrix}
\usage{
run_deseq2(
  counts,
  metadata,
  design = ~condition,
  contrast = NULL,
  shrink = TRUE,
  shrink_type = c("apeglm", "ashr", "normal"),
  min_count = 10,
  alpha = 0.05
)
}
\arguments{
\item{counts}{validated counts matrix (genes x samples, integer)}

\item{metadata}{data.frame with sample ID in column 1}

\item{design}{a one-sided formula referring to columns of \code{metadata},
e.g. \code{~ condition} or \code{~ batch + condition}. The variable of interest
should be the last term.}

\item{contrast}{a length-3 character vector: c(variable, level_treated,
level_reference). Example: \code{c("condition", "Treatment", "Control")}.}

\item{shrink}{logical, apply LFC shrinkage (recommended for visualization)}

\item{shrink_type}{shrinkage estimator: "apeglm" (default), "ashr", or "normal"}

\item{min_count}{minimum row sum to keep a gene (default 10)}

\item{alpha}{FDR threshold passed to \code{results()} for independent filtering}
}
\value{
a tidy data.frame with columns: gene, baseMean, log2FoldChange,
lfcSE, stat, pvalue, padj
}
\description{
Constructs a DESeqDataSet from counts and metadata, runs DESeq(), and
extracts results for a user-specified contrast. Optionally applies
LFC shrinkage (apeglm by default).
}
\examples{
\dontrun{
  counts <- read_counts("counts.csv")
  meta   <- read_metadata("metadata.csv", counts_samples = colnames(counts))
  res    <- run_deseq2(counts, meta,
                       design = ~ condition,
                       contrast = c("condition", "Treatment", "Control"))
}
}
