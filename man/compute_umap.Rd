% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fig_embed.R
\name{compute_umap}
\alias{compute_umap}
\title{Compute a 2D UMAP embedding of samples}
\usage{
compute_umap(
  counts_mat,
  n_top = 500,
  n_neighbors = 15,
  min_dist = 0.1,
  seed = 42
)
}
\arguments{
\item{counts_mat}{normalized counts matrix (genes x samples)}

\item{n_top}{number of top-variance genes to use}

\item{n_neighbors}{UMAP neighbourhood size (clamped to \code{n_samples - 1})}

\item{min_dist}{UMAP minimum distance}

\item{seed}{RNG seed for reproducibility}
}
\value{
list with \code{scores} (data.frame: UMAP1, UMAP2, sample), \code{n_used},
and the effective \code{n_neighbors}
}
\description{
Selects the top-variance genes and runs \code{\link[uwot:umap]{uwot::umap()}} on the samples
(samples as points, genes as features). Deterministic for a given \code{seed};
the global RNG state is saved and restored.
}
