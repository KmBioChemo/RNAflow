% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fig_compare.R
\name{fig_lfc_heatmap}
\alias{fig_lfc_heatmap}
\title{Cross-contrast log2FoldChange signature heatmap}
\usage{
fig_lfc_heatmap(
  contrasts,
  genes = NULL,
  gene_src = c("sig_union", "top_var"),
  n_genes = 50,
  padj_thr = 0.05,
  lfc_thr = 1,
  palette_name = "RdBu",
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  show_rownames = NULL,
  title = NULL,
  show_title = TRUE
)
}
\arguments{
\item{contrasts}{a named list of DE results data.frames}

\item{genes}{optional character vector to display (overrides gene_src)}

\item{gene_src}{"sig_union" (default; union of significant genes across
contrasts) or "top_var" (most variable genes among all)}

\item{n_genes}{max number of genes to display (top by cross-contrast
variance when more are available)}

\item{padj_thr, lfc_thr}{thresholds used when \code{gene_src == "sig_union"}}

\item{palette_name}{diverging palette name (default "RdBu")}

\item{cluster_rows, cluster_cols}{cluster genes / contrasts}

\item{show_rownames}{display gene labels (default: auto, when <= 60 genes)}

\item{title, show_title}{heatmap title}
}
\value{
a pheatmap object (gtable)
}
\description{
Genes x contrasts heatmap of log2FoldChange, for spotting shared vs.
contrast-specific transcriptional signatures. Genes absent from a
contrast are filled with 0 (no change) so clustering stays well-defined.
}
