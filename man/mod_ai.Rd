% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_ai.R
\name{mod_ai}
\alias{mod_ai}
\alias{mod_ai_ui}
\alias{mod_ai_server}
\title{AI interpretation module}
\usage{
mod_ai_ui(id)

mod_ai_server(
  id,
  de_reactive,
  enrich_reactive = NULL,
  organism_reactive = NULL,
  contrast_params_reactive = NULL,
  settings_store = NULL
)
}
\arguments{
\item{id}{namespace ID}

\item{de_reactive}{reactive returning the active contrast DE data.frame}

\item{enrich_reactive}{optional reactive returning the latest enrichment
result list (\code{method}, \code{table}), as exposed by \code{\link[=mod_enrich_server]{mod_enrich_server()}}}

\item{organism_reactive}{optional reactive returning the organism keyword}

\item{contrast_params_reactive}{optional reactive returning the active
contrast's parameter list (design_var / treated / reference)}

\item{settings_store}{optional \code{reactiveVal} holding a settings list; the
latest interpretation is recorded under \verb{$ai_interpretation} so it can be
archived in the HTML report}
}
\description{
Shiny module wrapping the pure \link{ai_interpret} layer. Sends a compact summary
of the active contrast (top gene names + fold-changes, and the latest
enrichment terms) to Anthropic's Claude API and renders the returned
biological narrative. The API key lives only in the session -- it is read
from a password field or the \code{ANTHROPIC_API_KEY} environment variable, and
is never written to disk.
}
