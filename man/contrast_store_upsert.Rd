% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_state.R
\name{contrast_store_upsert}
\alias{contrast_store_upsert}
\title{Insert or update a contrast in a contrast store}
\usage{
contrast_store_upsert(
  store,
  label,
  results,
  params = list(),
  created = Sys.time()
)
}
\arguments{
\item{store}{the current store (a named list; may be empty)}

\item{label}{contrast label (unique key)}

\item{results}{DE results data.frame}

\item{params}{named list of parameters used to compute the contrast}

\item{created}{optional timestamp (defaults to now)}
}
\value{
the updated store
}
\description{
A contrast store is a named list keyed by contrast label. Each entry holds
the DE results data.frame, the parameters used to compute it, and a
timestamp. Re-adding an existing label updates that entry in place
(keeping its position), so re-running the same contrast refreshes it
rather than duplicating it.
}
\keyword{internal}
